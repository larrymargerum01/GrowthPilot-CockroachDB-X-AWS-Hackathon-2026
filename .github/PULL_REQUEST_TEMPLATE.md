## What changed

<!-- One or two sentences. What does this PR actually do? -->

## Why

<!-- Link the Trello card, e.g. T8 · MemoryStore interface + in-memory stub -->

## How to test

<!-- Exact steps a reviewer can follow. "Run pytest backend/tests" or "start the app, go to /health, expect 200 with db: ok" -->

## Checklist

- [ ] Ran it locally and it works
- [ ] No secrets, `.env` files or certs committed
- [ ] Trello card moved to `In review`
- [ ] Anything that changes a shared interface (`MemoryStore`, `AgentContext`, DB schema) is flagged in the group chat
